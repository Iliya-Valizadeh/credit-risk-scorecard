# models/

`pipeline.joblib` (preprocessor + LightGBM + median template + threshold) is generated by
`python -m src.model` or the `03_run_all_colab.ipynb` notebook, and is gitignored
(binary artifact). The Streamlit app loads it from here.
